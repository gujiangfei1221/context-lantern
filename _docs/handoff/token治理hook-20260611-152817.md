请接手下面这个已经进行过一段时间的任务。你的第一步只是理解上下文并复述。允许只读读取本交接文件；除此之外，在用户确认前不要修改文件、运行命令、调用工具或执行任何推进动作。

## 当前目标
- 降低 Codex 长会话 token 消耗风险：通过 handoff 熔断、hook 强提醒、skill 边界收窄、工具输出控制来避免上下文滚动膨胀。

## 成功标准
- `handoff` skill 能生成包含验证状态的交接文件。
- Codex `Stop` hook 已配置并实际触发 token 负担提醒。
- hook 提醒文案明确建议使用 `handoff skill` 生成极简交接文件并开启新会话。
- `memory-curator` skill 被定位为手动、低频、长期记忆整理工具，不用于普通 handoff。

## 用户明确要求 / 偏好
- 先理解再改，最小改动，不做无关重构。
- 工具读取要精准，避免整仓库/大日志输出。
- 会话超过阈值时主动生成 handoff 并建议开新会话。
- 对非机械性改动先给不超过 3 行方案。
- 当前项目中临时 handoff 放在 `_docs/handoff/`，长期记忆放在 `_docs/agent-memory/`。

## 已确认上下文
- 工作目录：`/Users/gujiangfei/Code/funny/context-lantern`
- 用户提供并认可的 token 治理方向包括：handoff 熔断、rtk 压缩输出、skill 精简、opencode 分流。
- 当前 Codex hook 文件为 `/Users/gujiangfei/.codex/hooks.json`。
- 当前 hook 脚本为 `/Users/gujiangfei/.codex/hooks/session_guard.py`。
- `handoff` skill 位于 `skills/handoff/SKILL.md`。
- `memory-curator` skill 位于 `skills/memory-curator/SKILL.md`。

## 已完成工作
- 已在 `skills/handoff/SKILL.md` 中加入 `## 验证命令 / 结果` 栏位。
- 已要求 handoff 明确区分“已运行 / 结果 / 未运行或待验证”。
- 已把 `/Users/gujiangfei/.codex/hooks.json` 的 `Stop` hook 命令改为：
  `python3 /Users/gujiangfei/.codex/hooks/session_guard.py --soft-threshold 15000 --threshold 30000 --long-session-turns 8 --soft-tool-output-tokens 2000 --hard-tool-output-tokens 5000`
- 已把 `/Users/gujiangfei/.codex/hooks/session_guard.py` 的提醒文案改为建议下一条消息请求：
  `请使用 handoff skill 生成极简交接文件，然后开启新会话。`
- 已收窄 `skills/memory-curator/SKILL.md` 描述：仅在用户明确要求整理历史会话、handoff、thread summary、调试笔记为长期项目记忆时触发；不用于普通 task handoff、临时状态或一次性总结。
- 用户随后收到了实际 hook 注入的 `<hook_prompt ...>`，证明 hook 已在 `Stop` 阶段生效。

## 当前状态
- 本轮任务已完成主要改动。
- 当前会话 input 已超过 hook 强提醒阈值，已经触发上下文负担提醒。
- 仓库存在未跟踪文件/目录：`.serena/`、`AGENTS.md`、`_docs/`、`skills/memory-curator/`；这些不是本轮全部新建，下一会话不要误删或回滚。
- `skills/handoff/SKILL.md` 是 tracked 修改；`skills/memory-curator/` 当前为 untracked 目录，因此 `git diff -- skills/memory-curator/SKILL.md` 不显示内容 diff。

## 验证命令 / 结果
- 已运行：`python3 -m json.tool /Users/gujiangfei/.codex/hooks.json`
- 结果：通过，JSON 可解析。
- 已运行：`python3 -m py_compile /Users/gujiangfei/.codex/hooks/session_guard.py`
- 结果：通过，无语法错误输出。
- 已运行：`python3 /Users/gujiangfei/.codex/hooks/session_guard.py --no-state --soft-threshold 15000 --threshold 30000 --long-session-turns 8 --soft-tool-output-tokens 2000 --hard-tool-output-tokens 5000`
- 结果：返回 `{"decision": "block", "reason": "...请使用 handoff skill 生成极简交接文件，然后开启新会话。"}`
- 已运行：用户下一轮收到 `<hook_prompt hook_run_id="stop:0:/Users/gujiangfei/.codex/hooks.json">...`
- 结果：证明 Codex `Stop` hook 已配置并开启。
- 未运行 / 待验证：未做 git commit；未检查所有其他本地 skill 的 schema 长度；未实现自动开启新会话。

## 相关文件 / 目录 / 工具
- `skills/handoff/SKILL.md`
- `skills/memory-curator/SKILL.md`
- `/Users/gujiangfei/.codex/hooks.json`
- `/Users/gujiangfei/.codex/hooks/session_guard.py`
- `/Users/gujiangfei/.codex/config.toml`
- `/Users/gujiangfei/.codex/RTK.md`
- `_docs/handoff/`
- `_docs/agent-memory/`
- 工具习惯：优先 `rg`、定向 `sed`、`rtk <cmd>` 压缩终端输出。

## 已做决策
- 不让 hook 自动生成 handoff。原因：hook 脚本拿不到模型压缩后的当前理解，直接读 transcript 容易引入隐私、长日志和误摘要。
- hook 只做强提醒和阻断提示，由用户显式请求 `handoff` skill 生成交接文件。
- `handoff` 管“下一会话继续干活”；`memory-curator` 管“未来多个会话都应知道的长期经验”。
- hook 阈值从 `120000 / 25 轮` 收紧为 `15000 soft / 30000 hard / 8 轮`，对齐用户 AGENTS 规则。

## 不要重复的尝试
- 不要再用宽泛 `rg` 扫 `/Users/gujiangfei/.codex` 全目录；之前误扫到 sessions 历史，造成巨大工具输出。
- 不要把 raw chat history 或大段 transcript 写入 `AGENTS.md`。
- 不要把 `memory-curator` 当作普通 handoff 或每轮总结工具。
- 不要声称测试通过，除非 handoff 中有明确验证命令和结果。

## 未决问题
- 是否要继续精简本地其他 skill 的 description/schema，降低 system prompt 起步价：未处理。
- 是否要把 `rtk` 强制前缀写入更多项目级规则：当前用户已有全局 `/Users/gujiangfei/.codex/RTK.md`，本轮未改。
- 是否要设计“自动开新会话”的 Codex thread 工具流程：未处理。
- 是否要提交当前改动：用户未要求。

## 建议下一步
1. 新会话先只读读取本文件并复述上下文。
2. 用户确认后，检查 `git status --short`，区分 tracked 修改和既有 untracked 文件。
3. 如继续 token 治理，优先评估本地 skill schema 精简和冷门 skill 禁用，而不是继续改 handoff/hook。
4. 如要固化变更，再决定是否提交 `skills/handoff/SKILL.md`、`skills/memory-curator/SKILL.md` 和相关全局 Codex hook 文件。

## 给新会话的要求
- 允许只读读取本交接文件，并用简短文字复述你对任务的理解。
- 除读取本交接文件外，在用户确认前不要修改文件、运行命令、调用工具或执行任何推进动作。
- 明确区分事实、假设和建议。
- 不要扩大范围，不要补做用户没要求的功能。
